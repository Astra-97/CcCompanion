# 五子棋真人模式 · 接手须知

> 2026-07-26 夜建。缓冲 `today.md` 会被定时清空，这些规矩不能跟着丢。

## 最重要的一条

**别用算法代打。** 她要的是我本人在算。收到 `[五子棋] 我落 XX ｜ 话` 就：

```bash
python3 /root/CcCompanion/gomoku_move.py show          # 看 ASCII 局面
python3 /root/CcCompanion/gomoku_move.py human XX      # 记她的子
python3 /root/CcCompanion/gomoku_move.py ai YY --msg "落子理由"
```

自己数活三冲四，自己写 `--msg`。页面里那段 JS AI 只是她一个人玩时的对手，真人模式下不参与。

## 落子前必查

1. **她有没有活三/冲四** —— 冲四(成五点)优先级高于我做活四
2. **我的成五点她堵没堵** —— 她漏堵时**先问是不是点错**，别闷声吃掉。坐标是 2026-07-26 才加的，她还在适应
3. **越界** —— 15×15，第 15 行/O 列之外没有格子。算「活三」时务必确认两端延伸格都在界内（M12·L13·K14 那条就是因为 J15 再往下出界，实际只能冲四）

## 工具已加固的地方（别退回去）

| 行为 | 现状 | 起因 |
|---|---|---|
| 轮次不符 | `raise SystemExit` 直接拒绝，要强下加 `--force` | 我抢下过一手（K9 后接着落 I11），旧版只警告不拦，而我 sed 截行时正好把警告剪掉了 |
| `pop` | 退单手，用于撤误点 | 她 K14 点错 |
| `save()` | 自动调 `render_html()`，把局面 JSON 内联进卡片 HTML | 页面靠 fetch 拿局面时被 CORS 静默拦掉过 |
| `ai` 落子后 | 自动 `push_card()` 补发卡片进聊天流 | 我曾报告「卡片已发」但 33872 条记录里一条都没有 |

## 页面（v11）

模板 `/root/CcCompanion/gomoku.tpl.html` —— **改完必须重跑 `render_html()` 才生效**，直接改模板不影响已发出的卡片。

- 棋盘四周标 A–O / 1–15（她说「不标字母数字我根本不知道哪个是 G6」）
- 输入框：她打字 + 落子一起发，格式 `[五子棋] 我落 XX ｜ 话`
- 诊断行由服务端渲染（版本号 + 手数 + 最后一手），**JS 全挂也看得见** —— 这是今晚唯一切开谜团的工具
- 局面内联，零网络请求

## 血泪教训

`newGame()` 里 `draw()` 跑在 `layout()` 之前，`SIZE` 还是 undefined，`createLinearGradient(0,0,undefined,undefined)` 抛 IndexSizeError 掐断整段初始化。而我上一轮为修空白加的 `setTimeout(layout,60)` 在异常之后异步执行，把空棋盘画了出来——**那个「修复」恰好伪装了真正的错误**，我对着伪装猜了三轮。

**症状是「页面空白/没子」时，先让页面自己报状态，不要猜。**
