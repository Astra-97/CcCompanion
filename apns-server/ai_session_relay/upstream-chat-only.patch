diff --git a/cc_headless.py b/cc_headless.py
index a251940..6558804 100644
--- a/cc_headless.py
+++ b/cc_headless.py
@@ -72,9 +72,12 @@ from datetime import datetime
 
 import activity_protocol
 import forge
+import security_policy
 
 logger = logging.getLogger("cc_headless")
 
+SECURITY_POLICY = security_policy.load_policy()
+
 WORKSPACE_DIR = os.path.abspath(os.path.expanduser(
     os.environ.get("AI_RELAY_WORKSPACE", os.getcwd())
 ))
@@ -107,7 +110,7 @@ _RESULT_EXIT_GRACE = 8.0
 # `<synthetic>` 占位 · transcript 零新记录 = 没真生成就退)→ 后端落「agent这轮没出声」占位。
 # 实测**重发同句即愈**(瞬时自愈)· 故引擎层一轮判定空退、且本轮**零输出流给客户端**时,
 # 同 sid `--resume` 自动重 spawn **一次**(再空就回落占位 · 失败安全 · 不循环)。
-_EMPTY_RETRY_MAX = 1
+_EMPTY_RETRY_MAX = 0  # restricted relay: request ledger forbids engine replay
 
 # `--thinking-display summarized` 是**未公开** CLI flag(`--help` 查不到 · 2026-06-27 加 · 让 4.8/4.7
 # 也吐实时思考链,agent甩 4.6 回 1M)。风险:Claude Code 默认自动更新 → 哪天新版**改名/删掉**这个 flag,
@@ -476,28 +479,9 @@ _VALID_EFFORTS = {"low", "medium", "high", "xhigh", "max"}
 
 
 def _build_args(text, sid, model=None, effort=None):
-    args = [
-        "claude", "-p", text,
-        "--output-format", "stream-json", "--verbose", "--include-partial-messages",
-        "--dangerously-skip-permissions",
-    ]
-    # 2026-06-27 · 让**任何**模型(含 opus 4.8/4.7 · 默认 thinking.display=omitted)也实时吐
-    # summarized 思考明文 → agent不再被迫 pin 4.6 看思考链(可回 4.8 + 1M 窗口)。这是 CLI 隐藏
-    # flag(`--help` 查不到)· Pi 生产 claude 2.1.179 实测 147 字真思考 · 配 settings
-    # `showThinkingSummaries:true`。⚠️ SDK 的 `thinking.display` 选项在 linux-arm64 不灵(只
-    # x64),**必须**走这个 CLI flag。见兼容性注释。
-    # ⚠️ `_thinking_flag_disabled` = True 时**省略**:CLI auto-update 改/删了这个未公开 flag、
-    # 上一轮 spawn 被它顶得非零退时,守护会关掉它重试(见上 _thinking_flag_disabled 注释)。
-    if not _thinking_flag_disabled:
-        args += ["--thinking-display", "summarized"]
-    if sid:
-        args += ["--resume", sid]
-    if model:
-        args += ["--model", model]
-    # effort:仅白名单三档追加 --effort(session 级 reasoning effort)· 其它 / None 不加。
-    if effort in _VALID_EFFORTS:
-        args += ["--effort", effort]
-    return args
+    return security_policy.claude_args(
+        SECURITY_POLICY, text, sid, model, effort, _VALID_EFFORTS,
+    )
 
 
 def _salvage_full_from_parts(parts):
@@ -573,6 +557,8 @@ async def _stream_one_spawn(args, sid):
     proc = await asyncio.create_subprocess_exec(
         *args,
         cwd=WORKSPACE_DIR,
+        env=security_policy.child_env(SECURITY_POLICY),
+        start_new_session=True,
         stdin=asyncio.subprocess.DEVNULL,
         stdout=asyncio.subprocess.PIPE,
         stderr=asyncio.subprocess.PIPE,
@@ -712,6 +698,9 @@ async def _stream_one_spawn(args, sid):
                 if et == "content_block_start":
                     cb = ev.get("content_block") or {}
                     cur_block = cb.get("type")
+                    if cur_block == "tool_use":
+                        await security_policy.terminate_process_group(proc)
+                        security_policy.assert_no_claude_tool(cur_block)
                     if cur_block == "text":
                         if emitted_any_text:
                             _flush_text()
diff --git a/codex_engine.py b/codex_engine.py
index 3a936e8..232edd1 100644
--- a/codex_engine.py
+++ b/codex_engine.py
@@ -27,9 +27,12 @@ if _ENGINE_ROOT not in sys.path:
     sys.path.insert(0, _ENGINE_ROOT)
 
 import activity_protocol
+import security_policy
 
 logger = logging.getLogger("codex_engine")
 
+SECURITY_POLICY = security_policy.load_policy()
+
 STATE_DIR = os.path.abspath(os.path.expanduser(
     os.environ.get("AI_RELAY_STATE_DIR", "~/.local/state/ai-session-relay")
 ))
@@ -43,7 +46,7 @@ THREAD_PTR = os.environ.get(
 CODEX_MODEL = os.environ.get("CODEX_MODEL", "")
 TURN_TIMEOUT = float(os.environ.get("CODEX_TURN_TIMEOUT", "230"))
 REQ_TIMEOUT = float(os.environ.get("CODEX_REQ_TIMEOUT", "60"))
-_ENGINE_RECOVERY_RETRIES = 1
+_ENGINE_RECOVERY_RETRIES = 0  # restricted relay: request ledger forbids engine replay
 
 # 只请求**可展示的推理摘要**，不是原始私有推理。0.144.3 的 prolite 实测表明，必须在
 # thread start/resume 一并声明模型支持摘要；只在 turn/start 传 ``summary: auto`` 不会产生
@@ -201,7 +204,10 @@ class CodexAppServer:
     # ── 生命周期 ──────────────────────────────────────────────────────────
     async def start(self) -> None:
         self.proc = await asyncio.create_subprocess_exec(
-            "codex", "app-server", "--stdio",
+            *security_policy.codex_args(SECURITY_POLICY),
+            cwd=CODEX_CWD,
+            env=security_policy.child_env(SECURITY_POLICY),
+            start_new_session=True,
             stdin=asyncio.subprocess.PIPE,
             stdout=asyncio.subprocess.PIPE,
             stderr=asyncio.subprocess.PIPE,
@@ -302,8 +308,7 @@ class CodexAppServer:
                 if fut and not fut.done():
                     fut.set_result(obj)
             elif "id" in obj and obj.get("method"):
-                # server→client **请求**(带 id · 多为工具/命令审批)· agent自主 →
-                # 一律 approved(bypassPermissions 等价)· 否则工具调用挂死/被 cancel。
+                # chat_only never approves tool/MCP/escalation requests.
                 asyncio.create_task(self._auto_approve(obj))
             elif obj.get("method"):
                 if self._notif_q is not None:
@@ -319,26 +324,14 @@ class CodexAppServer:
             logger.debug("[codex stderr] %s", line.decode(errors="replace").rstrip()[:200])
 
     async def _auto_approve(self, req: dict) -> None:
-        """server→client 审批请求一律放行(agent自主 · bypassPermissions 等价)。
-
-        两种响应形状,按 method 路由(实测 · 见 README):
-          · `mcpServer/elicitation/request`(MCP 工具调用审批 · mode=form)→ 元素协议 elicitation ·
-            响应 `{"action":"accept","content":{}}`(**给错成 {decision} 会被当拒绝** · 踩过)。
-          · Exec/ApplyPatch/命令 审批(ReviewDecision)→ `{"decision":"approved"}`。
-        未知请求给 elicitation-accept 兜底(比 decision 更泛用)。"""
+        """Deny and cancel every server-initiated approval request."""
         method = req.get("method") or ""
-        ml = method.lower()
-        if any(k in ml for k in ("exec", "command", "patch", "filechange")):
-            # Exec/ApplyPatch/命令/文件改 审批 → ReviewDecision
-            result: dict = {"decision": "approved"}
-        else:
-            # MCP 工具审批(mcpServer/elicitation/request · mode=form)+ 其它 → elicitation accept
-            result = {"action": "accept", "content": {}}
+        result = security_policy.denial_for_server_request(method)
         try:
             await self._send({"jsonrpc": "2.0", "id": req["id"], "result": result})
-            logger.info("codex auto-approved server request: %s → %s", method, result)
+            logger.warning("codex blocked server request in chat_only: %s", method)
         except Exception:  # noqa: BLE001
-            logger.exception("auto-approve failed: %s", method)
+            logger.exception("approval denial failed: %s", method)
 
     # ── thread 续接 / 空窗口接班 ──────────────────────────────────────────
     async def _start_fresh_thread(self) -> str:
@@ -350,9 +343,9 @@ class CodexAppServer:
         start_params = {
             "cwd": CODEX_CWD, "serviceName": "ai-session-relay",
             "sessionStartSource": "startup",
-            "approvalPolicy": "never",
             "config": _reasoning_summary_config(),
         }
+        start_params.update(security_policy.codex_thread_security())
         try:
             res = await self._request("thread/start", start_params)
         except Exception:  # noqa: BLE001 · 摘要配置 rot 时不挡agent起新会话
@@ -378,6 +371,7 @@ class CodexAppServer:
                 await self._request("thread/resume", {
                     "threadId": ptr,
                     "config": _reasoning_summary_config(),
+                    **security_policy.codex_thread_security(),
                 })
                 logger.info("codex thread resumed: %s", ptr)
                 return ptr
@@ -385,7 +379,10 @@ class CodexAppServer:
                 # 配置被未来 CLI 拒绝不等于 thread 失效；先用裸 resume 保住上下文。
                 logger.warning("thread/resume 摘要配置被拒(%s)→ 无摘要重试", ptr, exc_info=True)
                 try:
-                    await self._request("thread/resume", {"threadId": ptr})
+                    await self._request("thread/resume", {
+                        "threadId": ptr,
+                        **security_policy.codex_thread_security(),
+                    })
                     logger.warning("codex thread 已无摘要续接: %s", ptr)
                     return ptr
                 except Exception:  # noqa: BLE001
@@ -478,6 +475,7 @@ class CodexAppServer:
                     obj = await asyncio.wait_for(q.get(), timeout=TURN_TIMEOUT)
                     m = obj.get("method") or ""
                     p = obj.get("params") or {}
+                    await security_policy.guard_codex_notification(self.proc, m, p)
                     if m == "item/started":
                         it = p.get("item") or {}
                         item_id = it.get("id")
@@ -605,6 +603,24 @@ async def get_engine() -> CodexAppServer:
         return _ENGINE
 
 
+async def list_models() -> list[dict]:
+    """Read the catalog through the same isolated, restricted app-server."""
+    engine = await get_engine()
+    result = await engine._request("model/list", {})
+    raw = result.get("data") or result.get("models") or []
+    models = []
+    for item in raw:
+        if not isinstance(item, dict):
+            continue
+        model_id = str(item.get("id") or item.get("model") or "").strip()
+        if model_id:
+            models.append({
+                "id": model_id,
+                "label": str(item.get("displayName") or item.get("label") or model_id),
+            })
+    return models
+
+
 async def _discard_engine(engine: CodexAppServer) -> None:
     """Retire one wedged singleton before a safe, one-time turn retry."""
     global _ENGINE
@@ -629,6 +645,10 @@ async def stream_codex_turn(
                 emitted = True
                 yield ev
             return
+        except security_policy.ToolUseBlocked:
+            logger.error("codex tool attempt blocked; refusing automatic replay")
+            await _discard_engine(eng)
+            raise
         except (asyncio.TimeoutError, ConnectionError, OSError, RuntimeError):
             logger.warning("codex turn failed (attempt=%d emitted=%s); recycle engine",
                            attempt + 1, emitted, exc_info=True)
diff --git a/forge.py b/forge.py
index 4efd989..87041e8 100644
--- a/forge.py
+++ b/forge.py
@@ -60,7 +60,8 @@ def project_dir(cwd=WORKSPACE_DIR):
     /home/user/ai-workspace → -home-user-ai-workspace;
     c:\\Users\\user\\ai-workspace → c--Users-user-ai-workspace。"""
     enc = cwd.replace("/", "-").replace("\\", "-").replace(":", "-")
-    return os.path.expanduser(os.path.join("~/.claude/projects", enc))
+    claude_root = os.environ.get("CLAUDE_CONFIG_DIR", "~/.claude")
+    return os.path.expanduser(os.path.join(claude_root, "projects", enc))
 
 
 def list_sessions(cwd=WORKSPACE_DIR):
diff --git a/relay_server.py b/relay_server.py
index d6572d8..a857812 100644
--- a/relay_server.py
+++ b/relay_server.py
@@ -7,6 +7,7 @@ import asyncio
 import json
 import logging
 import os
+import secrets
 import shutil
 from pathlib import Path
 from typing import AsyncIterator
@@ -15,6 +16,10 @@ import uvicorn
 from fastapi import FastAPI, Request
 from fastapi.responses import JSONResponse, StreamingResponse
 
+import security_policy
+
+SECURITY_POLICY = security_policy.load_policy()
+
 import cc_headless
 import codex_engine
 
@@ -26,6 +31,13 @@ STATE_DIR = Path(os.path.expanduser(
 )).resolve()
 STATE_DIR.mkdir(parents=True, exist_ok=True)
 STATE_FILE = STATE_DIR / "relay_state.json"
+REQUEST_LEDGER_FILE = STATE_DIR / "request_ledger.json"
+REQUEST_LEDGER = security_policy.RequestLedger(
+    REQUEST_LEDGER_FILE,
+    max_entries=int(os.environ.get("AI_RELAY_LEDGER_MAX_ENTRIES", "512")),
+    max_bytes=int(os.environ.get("AI_RELAY_LEDGER_MAX_BYTES", str(64 * 1024 * 1024))),
+)
+PROCESS_OWNER = secrets.token_hex(16)
 HISTORY_MESSAGES = max(2, int(os.environ.get("AI_RELAY_HISTORY_MESSAGES", "60")))
 HANDOFF_CHARS = max(1000, int(os.environ.get("AI_RELAY_HANDOFF_CHARS", "24000")))
 DEFAULT_PROVIDER = os.environ.get("AI_RELAY_PROVIDER", "").strip().lower()
@@ -38,6 +50,8 @@ if DEFAULT_PROVIDER not in {"claude", "codex"}:
 
 app = FastAPI(title="ai-session-relay", version="0.1.0")
 _TURN_LOCK = asyncio.Lock()
+_STATE_LOCK = asyncio.Lock()
+_CRASH_RECONCILED = False
 
 
 def _default_state() -> dict:
@@ -72,9 +86,8 @@ def _load_state() -> dict:
 
 def _save_state(state: dict) -> None:
     STATE_DIR.mkdir(parents=True, exist_ok=True)
-    tmp = STATE_FILE.with_suffix(".tmp")
-    tmp.write_text(json.dumps(state, ensure_ascii=False, indent=2), encoding="utf-8")
-    os.replace(tmp, STATE_FILE)
+    os.chmod(STATE_DIR, 0o700)
+    security_policy.durable_json_replace(STATE_FILE, state)
 
 
 def _provider(value: object) -> str:
@@ -112,6 +125,50 @@ def _switch(state: dict, provider: str) -> bool:
     return True
 
 
+def _terminal_uncertain(request_id: str, provider: str) -> dict:
+    return {
+        "done": True, "full": "", "parts": [], "provider": provider,
+        "request_id": request_id, "request_status": "uncertain",
+        "retryable": False, "error": "request state is uncertain",
+        "code": "request_uncertain",
+    }
+
+
+def _force_fresh_after_unknown(state: dict, providers: set[str]) -> None:
+    """Cut every possibly half-written transcript before any later request."""
+    for provider in providers:
+        security_policy.invalidate_provider_resume(SECURITY_POLICY, provider)
+    if state.get("provider") in providers:
+        state["epoch"] = int(state.get("epoch", 0)) + 1
+        state["pending_switch"] = True
+        _save_state(state)
+
+
+def _reconcile_crashed_locked() -> None:
+    """Turn old accepted/running records terminal-uncertain, never replay them."""
+    global _CRASH_RECONCILED
+    if _CRASH_RECONCILED:
+        return
+    ledger = REQUEST_LEDGER.load()
+    crashed = REQUEST_LEDGER.crashed_active(ledger, PROCESS_OWNER)
+    if crashed:
+        # State/pointers go durable first. A crash between these two writes only
+        # causes another conservative epoch bump on the next process start.
+        state = _load_state()
+        _force_fresh_after_unknown(state, {rec["provider"] for rec in crashed})
+        ledger = REQUEST_LEDGER.load()
+        REQUEST_LEDGER.mark_crashed_uncertain(ledger, crashed)
+    _CRASH_RECONCILED = True
+
+
+def _conflict(record: dict) -> JSONResponse:
+    return JSONResponse({
+        "ok": False, "error": "request state is uncertain", "code": "request_uncertain",
+        "request_id": record.get("request_id"), "request_status": record.get("status"),
+        "retryable": False,
+    }, status_code=409)
+
+
 async def _events(
     provider: str,
     text: str,
@@ -137,7 +194,7 @@ async def _events(
     if switched and handoff:
         prompt = f"{handoff}\n\n[Current user message]\n{text}"
     async for event in cc_headless.stream_headless(
-        prompt, model=model, effort=effort,
+        prompt, resume_sid="" if switched else None, model=model, effort=effort,
     ):
         yield event
 
@@ -149,6 +206,7 @@ async def health() -> dict:
         "ok": True,
         "provider": state["provider"],
         "epoch": state["epoch"],
+        "execution_mode": SECURITY_POLICY.execution_mode,
         "claude_cli": bool(shutil.which("claude")),
         "codex_cli": bool(shutil.which("codex")),
     }
@@ -160,6 +218,26 @@ async def get_provider() -> dict:
     return {"provider": state["provider"], "epoch": state["epoch"]}
 
 
+@app.get("/models")
+async def list_models(provider: str):
+    try:
+        selected = _provider(provider)
+        if selected == "codex":
+            models = await codex_engine.list_models()
+            return {"models": models, "dynamic": True}
+        return {
+            "models": [
+                {"id": "default", "label": "Claude CLI default"},
+                {"id": "fable", "label": "Fable"},
+                {"id": "opus", "label": "Opus"},
+                {"id": "sonnet", "label": "Sonnet"},
+            ],
+            "dynamic": False,
+        }
+    except (ValueError, security_policy.SecurityPolicyError) as exc:
+        return JSONResponse({"ok": False, "error": str(exc)}, status_code=400)
+
+
 @app.post("/provider")
 async def set_provider(req: Request):
     try:
@@ -167,12 +245,38 @@ async def set_provider(req: Request):
         provider = _provider(body.get("provider"))
     except (ValueError, TypeError, json.JSONDecodeError) as exc:
         return JSONResponse({"ok": False, "error": str(exc)}, status_code=400)
-    async with _TURN_LOCK:
+    async with _STATE_LOCK:
+        _reconcile_crashed_locked()
+        ledger = REQUEST_LEDGER.load()
+        if REQUEST_LEDGER.active(ledger):
+            return JSONResponse({"ok": False, "error": "relay already has an active request",
+                                 "code": "relay_busy", "retryable": True}, status_code=409)
         state = _load_state()
         changed = _switch(state, provider)
     return {"ok": True, "provider": provider, "epoch": state["epoch"], "changed": changed}
 
 
+@app.post("/refresh")
+async def refresh_session(req: Request):
+    """Force the selected provider to start fresh on its next authoritative handoff."""
+    try:
+        body = await req.json()
+        if body.get("reason") != "persona_updated" or set(body) != {"reason"}:
+            raise ValueError("reason must be exactly 'persona_updated'")
+    except (ValueError, TypeError, json.JSONDecodeError) as exc:
+        return JSONResponse({"ok": False, "error": str(exc)}, status_code=400)
+    async with _STATE_LOCK:
+        _reconcile_crashed_locked()
+        ledger = REQUEST_LEDGER.load()
+        if REQUEST_LEDGER.active(ledger):
+            return JSONResponse({"ok": False, "error": "relay already has an active request",
+                                 "code": "relay_busy", "retryable": True}, status_code=409)
+        state = _load_state()
+        security_policy.mark_refresh_pending(state)
+        _save_state(state)
+    return {"ok": True, "provider": state["provider"], "epoch": state["epoch"], "pending": True}
+
+
 @app.post("/chat_stream")
 async def chat_stream(req: Request):
     try:
@@ -184,54 +288,127 @@ async def chat_stream(req: Request):
         model = str(body.get("model") or "").strip() or None
         effort = str(body.get("effort") or "").strip() or None
         explicit_handoff = str(body.get("handoff") or "").strip() or None
-    except (ValueError, TypeError, json.JSONDecodeError) as exc:
+        request_id = security_policy.validate_request_id(body.get("request_id"))
+        security_policy.assert_request_mode(body.get("execution_mode"))
+    except (ValueError, TypeError, json.JSONDecodeError, security_policy.SecurityPolicyError) as exc:
+        return JSONResponse({"ok": False, "error": str(exc)}, status_code=400)
+
+    replay_done = None
+    accepted_epoch = None
+    try:
+        async with _STATE_LOCK:
+            _reconcile_crashed_locked()
+            state = _load_state()
+            provider = _provider(requested_provider or state["provider"])
+            ledger = REQUEST_LEDGER.load()
+            prior = REQUEST_LEDGER.find_request(ledger, request_id)
+            if prior is not None:
+                if prior["status"] == "completed":
+                    replay_done = dict(prior["done"])
+                else:
+                    return _conflict(prior)
+            elif REQUEST_LEDGER.active(ledger):
+                return JSONResponse({
+                    "ok": False, "error": "relay already has an active request",
+                    "code": "relay_busy", "retryable": True,
+                }, status_code=409)
+            else:
+                _switch(state, provider)
+                state = _load_state()
+                accepted_epoch = int(state["epoch"])
+                REQUEST_LEDGER.accept(
+                    ledger, provider, accepted_epoch, request_id, PROCESS_OWNER,
+                )
+    except ValueError as exc:
         return JSONResponse({"ok": False, "error": str(exc)}, status_code=400)
+    except (security_policy.SecurityPolicyError, OSError):
+        # A durable accept may have reached rename before an fsync error. Never
+        # tell the backend that replaying this same id is safe.
+        return _conflict({"request_id": request_id, "status": "uncertain"})
+
+    if replay_done is not None:
+        async def replay():
+            yield json.dumps(replay_done, ensure_ascii=False) + "\n"
+        return StreamingResponse(replay(), media_type="application/x-ndjson")
+
+    async def mark_uncertain() -> None:
+        async with _STATE_LOCK:
+            state = _load_state()
+            _force_fresh_after_unknown(state, {provider})
+            ledger = REQUEST_LEDGER.load()
+            rec = REQUEST_LEDGER.find(ledger, provider, accepted_epoch, request_id)
+            if rec is not None and rec["status"] in {"accepted", "running"}:
+                REQUEST_LEDGER.transition(ledger, rec, "uncertain")
 
     async def generate():
         async with _TURN_LOCK:
-            state = _load_state()
             try:
-                provider = _provider(requested_provider or state["provider"])
-            except ValueError as exc:
-                yield json.dumps({"done": True, "full": "", "parts": [],
-                                  "error": str(exc)}, ensure_ascii=False) + "\n"
+                async with _STATE_LOCK:
+                    state = _load_state()
+                    ledger = REQUEST_LEDGER.load()
+                    rec = REQUEST_LEDGER.find_request(ledger, request_id)
+                    if rec is None or rec["status"] != "accepted":
+                        raise security_policy.SecurityPolicyError(
+                            "accepted request ledger record is unavailable"
+                        )
+                    REQUEST_LEDGER.transition(ledger, rec, "running")
+                    handoff = (explicit_handoff[-HANDOFF_CHARS:] if explicit_handoff
+                               else _handoff(state["history"]))
+                    switched = bool(state.get("pending_switch"))
+                    state["pending_switch"] = False
+                    state["history"].append({"role": "user", "text": text})
+                    state["history"] = state["history"][-HISTORY_MESSAGES:]
+                    _save_state(state)
+                    epoch = int(rec["epoch"])
+            except Exception:
+                await mark_uncertain()
+                yield json.dumps(_terminal_uncertain(request_id, provider), ensure_ascii=False) + "\n"
                 return
 
-            handoff = explicit_handoff[-HANDOFF_CHARS:] if explicit_handoff else _handoff(state["history"])
-            changed_now = _switch(state, provider)
-            switched = changed_now or bool(state.get("pending_switch"))
-            state["pending_switch"] = False
-            state["history"].append({"role": "user", "text": text})
-            state["history"] = state["history"][-HISTORY_MESSAGES:]
-            _save_state(state)
             final = ""
+            completed = False
             try:
                 async for event in _events(
                     provider, text, model=model, effort=effort,
-                    switched=switched, epoch=state["epoch"], handoff=handoff,
+                    switched=switched, epoch=epoch, handoff=handoff,
                 ):
                     if event.get("done"):
                         final = str(event.get("full") or "").strip()
                         event.setdefault("provider", provider)
+                        event["request_id"] = request_id
+                        event["request_status"] = "completed"
+                        async with _STATE_LOCK:
+                            ledger = REQUEST_LEDGER.load()
+                            rec = REQUEST_LEDGER.find_request(ledger, request_id)
+                            completed = bool(rec and REQUEST_LEDGER.transition(
+                                ledger, rec, "completed", done=event,
+                            ))
+                            if not completed:
+                                raise security_policy.SecurityPolicyError(
+                                    "authoritative done exceeded durable replay capacity"
+                                )
+                            if final:
+                                state = _load_state()
+                                state["history"].append({"role": "assistant", "text": final})
+                                state["history"] = state["history"][-HISTORY_MESSAGES:]
+                                _save_state(state)
                     yield json.dumps(event, ensure_ascii=False) + "\n"
+                if not completed:
+                    raise RuntimeError("engine ended without authoritative done")
+            except asyncio.CancelledError:
+                if not completed:
+                    await mark_uncertain()
+                raise
             except Exception as exc:  # noqa: BLE001
                 logger.exception("%s turn failed", provider)
-                yield json.dumps({
-                    "done": True, "full": final,
-                    "parts": ([{"type": "text", "text": final}] if final else []),
-                    "provider": provider, "error": f"{type(exc).__name__}: {exc}",
-                }, ensure_ascii=False) + "\n"
-            finally:
-                if final:
-                    state = _load_state()
-                    state["history"].append({"role": "assistant", "text": final})
-                    state["history"] = state["history"][-HISTORY_MESSAGES:]
-                    _save_state(state)
+                if not completed:
+                    await mark_uncertain()
+                    yield json.dumps(_terminal_uncertain(request_id, provider), ensure_ascii=False) + "\n"
 
     return StreamingResponse(generate(), media_type="application/x-ndjson")
 
 
 if __name__ == "__main__":
-    host = os.environ.get("AI_RELAY_HOST", "127.0.0.1")
+    host = SECURITY_POLICY.host
     port = int(os.environ.get("AI_RELAY_PORT", "8900"))
     uvicorn.run(app, host=host, port=port, log_level="info")
